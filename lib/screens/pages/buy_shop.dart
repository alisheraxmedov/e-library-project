import 'package:flutter/material.dart';

class BuyShop extends StatelessWidget {
  const BuyShop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Buy and Shop"),
      ),
    );
  }
}
